spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  base-url: http://localhost:8080
  rate-limit:
    enabled: false

logging:
  level:
    com.assessment.urlshortener: WARN
